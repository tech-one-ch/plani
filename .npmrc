# Hoist all packages to the root node_modules so webpack (Next.js production
# build) can resolve transitive deps across workspace package boundaries.
# Without this, pnpm strict mode breaks better-auth subpath imports
# (better-auth/react, better-auth/client/plugins) when @plani/auth is compiled
# via transpilePackages.
shamefully-hoist=true
